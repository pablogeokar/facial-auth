services:
  - type: web
    name: facial-auth-api
    runtime: node
    region: oregon
    rootDir: api
    buildCommand: pnpm install && pnpm run build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST
        value: 0.0.0.0
